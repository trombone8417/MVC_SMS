@model DatabaseAccess.DesignationTable

@{
    ViewBag.Title = "Designation";
}

<h2>Details</h2>

<div>
    <h4>職稱</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            職稱
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            狀態
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsActive)
        </dd>

        <dt>
            登入人員
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserTable.FullName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("更新", "Edit", new { id = Model.DesignationID }, new { @class = "btn btn-primary" })
    @Html.ActionLink("回主頁", "Index", null, new { @class = "btn btn-default" })
</p>
